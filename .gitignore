# Secretos. Esta línea es la razón por la que .gitignore se escribió antes del primer commit.
.env
.env.*
!.env.example

# Datos. El corpus no vive en git: data/raw/ pesa cientos de MB y es inmutable,
# se respalda aparte. Ver CLAUDE.md, sección Respaldo.
data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# Modelos y cachés de embeddings descargados
.cache/
*.safetensors

# Entorno y editor
.venv/
venv/
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Material de terceros compartido en confianza. NUNCA se publica ni se comparte.
# Se lee localmente para contrastar resultados; no entra al repo ni a ningún
# archivo derivado que salga de esta máquina. Ver HANDOFF.md.
assets/
